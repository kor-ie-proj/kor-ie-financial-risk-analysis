# .env

# --- MySQL Database Settings ---
# MySQL 컨테이너가 사용할 루트 비밀번호와 생성할 데이터베이스 이름
MYSQL_ROOT_PASSWORD=1234
MYSQL_DATABASE=IE_project

# --- MinIO S3 Storage Settings ---
# MinIO 컨테이너가 사용할 관리자 계정 정보 (최신 MinIO는 ROOT_USER/PASSWORD 사용)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadminpass

# 다른 서비스(MLflow, Training, Inference)가 MinIO에 연결할 때 사용할 AWS 표준 환경변수
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadminpass

# MLflow가 아티팩트를 저장할 버킷 이름
MLFLOW_ARTIFACT_BUCKET=mlflow-artifacts

# --- Application Connection URIs ---
# Training 및 Inference 서버가 DB에 연결할 때 사용할 전체 주소 (Connection String)
# docker-compose 내부에서는 'mysql'이라는 서비스 이름으로 DB에 접근.
DB_URI=mysql+pymysql://root:${MYSQL_ROOT_PASSWORD}@mysql:3306/${MYSQL_DATABASE}?charset=utf8mb4

# --- Backend & Frontend Integration ---
# FastAPI 통합 백엔드에서 사용할 구성 값
ALLOWED_ORIGINS=http://localhost:3000
INFERENCE_SERVER_URL=http://localhost:8001

# --- External API Keys ---
# 데이터 수집이나 다른 서비스에 필요할 수 있는 API 키
DART_API_KEY=YOUR_DART_API_KEY_HERE
ECOS_API_KEY=YOUR_ECOS_API_KEY_HERE
